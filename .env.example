# Team Admin API key (admin:*) from https://cursor.com/dashboard → API Keys
# For local runs: copy to .env (gitignored)
# For Cloud Agents / Automations: set CURSOR_API_KEY as an environment / automation secret — do not commit the real value
CURSOR_API_KEY=
